{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "tab-webviews",
  "description": "Capabilities for dynamically created tab webviews (link interceptor IPC)",
  "windows": ["main"],
  "webviews": ["tab-*"],
  "permissions": [
    "core:default"
  ]
}
